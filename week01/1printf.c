#include <stdio.h>

int main () {

    
    // cout << "Hello, world!" << endl;
    printf("Hello, world!\n");

	int x=1;
	float pi=3.14;

    // cout << "x = " << x << "pi = " << pi << endl;
	printf("x = %d, pi = %f\n", x, y);

    /*****************************************
     * Use the appropriate format specifier for each type.
     * The number, type, and order of variables should match those of
     * the format specifiers.
    *****************************************/

	return 0;
}
